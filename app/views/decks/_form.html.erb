<%= simple_form_for @deck do |f| %>
  <br>
    <%= f.label t("deck.title") %>
    <p> <%= f.input :title, label: false %>
  <br>     
    <%= f.button :submit %>
    <p> <%= f.full_error :token %>
<% end %>
