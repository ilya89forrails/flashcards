<h1><%= @card.original_text %></h1>

<p>Original text: <%= @card.original_text %></p>
<p>Translated text: <%= @card.translated_text %></p>
<p>Review date: <%= @card.review_date %></p>
<p><%= image_tag @card.pic_url(:thumb).to_s %>
<p><%= link_to "Edit", edit_deck_card_path(@card.deck_id) %>
<p><%= link_to "Delete", deck_card_path(@card.deck_id), :method => :delete, :confirm => "Are you sure?" %>
<p><%= link_to "Back", deck_path(@card.deck_id) %>
